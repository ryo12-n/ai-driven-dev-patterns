## 1. プロジェクト初期化

- [x] 1.1 `src/` ディレクトリに `go.mod` を作成する（モジュール名: `md-heading-cli`）
- [x] 1.2 ディレクトリ構成を作成する（`src/main.go`、`src/parser/`）

## 2. 見出し解析ロジック（parser パッケージ）

- [x] 2.1 `src/parser/parser.go` に `Heading` 構造体（Level int, Text string）を定義する
- [x] 2.2 `src/parser/parser.go` に `Parse(r io.Reader) ([]Heading, error)` 関数を実装する。行単位で走査し、ATX形式の見出しを検出する
- [x] 2.3 コードブロック（``` で始まる行）の開閉状態を追跡し、コードブロック内の `#` を見出しとして扱わないようにする

## 3. CLI エントリポイント

- [x] 3.1 `src/main.go` に `main()` 関数を実装する。引数チェック → ファイル読み込み → parser.Parse() 呼び出し → 結果出力の流れ
- [x] 3.2 引数なしの場合は usage メッセージを stderr に出力して終了コード1で終了する
- [x] 3.3 ファイルが存在しない場合はエラーメッセージを stderr に出力して終了コード1で終了する
- [x] 3.4 見出しをレベルに応じたインデント（(Level-1)*2 スペース）付きで stdout に出力する

## 4. テスト

- [x] 4.1 `src/parser/parser_test.go` に正常系テストを作成する（複数レベルの見出し検出）
- [x] 4.2 コードブロック内の `#` が除外されることのテストを作成する
- [x] 4.3 見出しが存在しないファイルのテストを作成する（空のスライスが返ること）
- [x] 4.4 `go build ./...` でビルドが成功することを確認する
- [x] 4.5 `go test ./...` で全テストがパスすることを確認する
